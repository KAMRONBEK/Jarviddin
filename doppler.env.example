# Doppler: create two configs (e.g. `dev` and `prd`) and import the matching file:
# - .env.dev.example  → dev config (local, long polling)
# - .env.prd.example  → prd config (VPS, webhook)
#
# Or paste KEY=value lines from those files into the dashboard Import.
