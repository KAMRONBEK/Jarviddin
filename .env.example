# Environment templates (pick one profile):
# - .env.dev.example  — local development (long polling, relaxed validation)
# - .env.prd.example  — production VPS (HTTPS webhook, full validation)
#
# Copy the file you need to `.env` and fill secrets, or import into Doppler `dev` / `prd`.
